# 把本文件复制为同目录下的 `test-command`（无扩展名）即可启用测试基线 Stop 门。
# 规则：第一行非注释内容会被当作 shell 命令执行；退出码非 0 视为基线红灯。
# 建议填快速的核心测试集，而不是完整的慢速套件（Stop 门每个代码回合都会跑）。
#
# 示例（按你的项目取一行，删掉行首 # ）：
# npm test -- --silent
# pnpm vitest run --reporter=dot
# pytest -q
# go test ./...
# cargo test --quiet
